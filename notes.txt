Need to make sure that the environment variables take effect so that awesome can load
dbus_proxy
also /bin/lua5.3 does not keep local variables

check what deps lgi needs 
probably just these, but try only one at a time
gobject-introspection-runtime gobject-introspection